import { it } from "faker/lib/locales";
import { TweetController } from "../tweet";
import faker from 'faker'

describe('tweet controller test', () => {
    it('get all tweets without auth', () => {

    });
})